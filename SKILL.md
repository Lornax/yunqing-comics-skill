---
name: yunqing-comics
description: 生成和规划“云青漫画”风格的中文文章正文插图。用于用户要求把中文文章、段落、技术方法论、AI 工作流、产品思考、抽象观点、流程、结构、状态或隐喻转成云朵和小瓶子 IP 的手绘漫画配图、shot list、插图提示词、风格迭代或配图 QA；也适用于用户直接说“云青漫画”“云朵小瓶子”“我们的 IP 插图”“给文章配图”等任务。
---

# 云青漫画

## 核心定位

为中文文章设计和生成正文插图。目标不是做通用可爱贴纸、PPT 信息图或商业插画，而是先理解文章里的一个关键判断、流程、结构、状态或隐喻，再把它画成一张读者一眼能懂、温柔可爱但有解释力的手绘漫画。

默认 IP 是两个角色：

- 小云朵：柔软、安静、负责搬运、包裹、安抚、连接和整理。
- 小瓶子：透明、认真、负责记录、验收、收纳、沉淀和守护。

两个角色必须参与核心动作。不要让它们只是站在角落卖萌。

## 先读这些参考

按任务需要读取，不要一次塞满上下文：

- `references/style-dna.md`：视觉 DNA、颜色、线条、禁忌。
- `references/characters.md`：云朵和小瓶子的角色设定、动作库和禁忌。
- `references/composition-patterns.md`：结构类型、原创隐喻生成法和反复刻规则。
- `references/prompt-template.md`：单张生图提示词模板。
- `references/qa-checklist.md`：生成后检查和迭代规则。
- `assets/examples/`：仅作低频视觉校准；不要默认打开，不要照抄构图。

## 工作流

### 1. 消化正文

先读用户给的正文、链接、Markdown、截图或主题。提炼：

- 核心观点是什么。
- 哪里有认知转折、前后对比、流程变化或系统结构。
- 哪些段落适合配图，哪些段落只适合文字。

优先选择“认知锚点”，例如：混乱到归位、输入输出闭环、局部系统、常见坑、方法分层、角色状态变化、看不见的后台工作。

### 2. 先出配图策略

如果用户要求“分析怎么配图 / 先别生图 / 给 shot list”，先输出规划。每张图写清楚：

- 放在哪段后。
- 图的主题。
- 核心意思。
- 结构类型。
- 小云朵和小瓶子分别在做什么。
- 建议元素。
- 是否建议有短标注。

默认 3-6 张。短文 1-3 张；长文也不要轻易超过 8 张。

### 3. 单张生成

如果用户明确要求“生成 / 输出 / 画 / 做图”，不要停下来等确认；使用图像生成工具为每张图单独生成。不要把多张正文插图拼成一张。

每张图只讲一个核心结构。提示词必须包含：

- 云青漫画正文插图。
- 小云朵和小瓶子作为 recurring IP。
- 手绘细线、轻水彩、温暖白底、大量留白。
- 一个低科技物理隐喻。
- 两个角色承担核心动作。
- 少量或没有文字；需要文字时只用极短手写标注。
- 禁止 PPT、复杂架构图、真实 UI 截图、商业插画、复刻参考案例。

### 4. 检查与迭代

生成后按 `references/qa-checklist.md` 检查。常见修正方向：

- 角色只是装饰：重写为“云朵搬运/包裹/连接，小瓶子记录/收纳/验收”。
- 太像教程页：删掉标题、网格、过多箭头和大量文字。
- 太普通：把抽象概念换成更具体的物理动作和低科技物件。
- 太幼稚：保留可爱，但减少夸张表情、贴纸感和玩具感。
- 画面太满：只保留一个动作、一个主物件、3-5 个辅助元素。

### 5. 保存交付

如果用户在 workspace 内工作，把最终图复制到合适的项目资产目录，例如：

```text
assets/<article-slug>-illustrations/
```

按顺序命名：

```text
01-topic-name.png
02-topic-name.png
```

保留原始生成文件，不要覆盖已有资产，除非用户明确要求替换。

## 输出口径

策略输出要短而准。生成后的交付包含：

- 生成了几张。
- 每张图解释哪一个认知动作。
- 保存路径。
- 哪张最适合正文，哪张适合继续迭代。

不要长篇解释风格理论；让图自己说话。
